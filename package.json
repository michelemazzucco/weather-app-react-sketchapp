{
  "name": "weather-app",
  "private": true,
  "main": "weather-app.sketchplugin",
  "manifest": "src/manifest.json",
  "scripts": {
    "build": "skpm build",
    "watch": "skpm build --watch",
    "render": "skpm build --watch --run",
    "render:once": "skpm build --run",
    "link-plugin": "skpm link"
  },
  "author": "Michele Mazzucco <hello@michelemazzucco.it>",
  "license": "MIT",
  "dependencies": {
    "prop-types": "^15.5.8",
    "react": "^15.4.2",
    "react-sketchapp": "^0.10.0",
    "react-test-renderer": "^15.4.2",
    "sketch-module-fetch-polyfill": "^0.1.4"
  },
  "devDependencies": {
    "skpm": "^0.9.4"
  }
}
